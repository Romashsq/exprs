doctype html
html
  head
    title Список пользователей
    link(rel="stylesheet", href="/styles.css")
  body
    h1 Список пользователей
    ul
      each user in users
        li
          a(href=`/users/${user.id}`) #{user.name} (#{user.age} лет)
